# TODO: Portal de Empleos Local (Minú Empleos)

## 1. Configuración Inicial y Migración (✓ COMPLETADO)
[x] Instalar Next.js con App Router, Tailwind CSS, y TypeScript.
[x] Migrar toda la base de datos y esquema de "Marketplace" a "Portal de Empleos" (Companies, Jobs, Applications).
[x] Rebranding completo a "Minú Empleos" (Navbar, Footer, Hero, Meta tags).
[x] Integración de base de datos Supabase (Server Actions para companies, jobs, applications, categories, localities).

## 2. UI y Componentes (En Progreso)
[x] Layout principal con buscador integrado en el Navbar.
[x] Tarjetas de Empleo (`JobCard.tsx`) y Tarjetas de Empresa (`CompanyCard.tsx`).
[x] Botón de postulación interactivo (`ApplyJobButton.tsx`).
[x] Formularios de creación: `CreateJobForm.tsx` y `CompanySettingsForm.tsx`.
[x] Crear componentes UI genéricos reutilizables (Button, Input, Modal) para estandarizar el diseño y reducir clases de Tailwind repetidas.
[x] Añadir estados de carga (`loading.tsx` o skeletons) en las vistas que hacen fetch a Supabase.

## 3. Rutas de Candidatos (En Progreso)
[x] `app/page.tsx` (Home): Hero con buscador y categorías.
[x] `app/empleos/[slug]/page.tsx`: Detalle de la oferta de empleo y botón de postularse.
[x] `app/empresas/[id]/page.tsx`: Perfil público de la empresa y lista de vacantes.
[x] `app/mis-postulaciones/page.tsx`: Seguimiento de estado de postulaciones enviadas.
[x] `app/iniciar/page.tsx` y `app/registro/page.tsx`: Autenticación y creación de perfiles de usuario.
[x] `app/mi-cuenta/page.tsx`: Formulario para que el usuario gestione su perfil y suba su currículum (CV) al bucket `RESUMES` de Supabase.
[ ] `app/mi-cuenta/eliminar-cuenta/page.tsx`: Crear el formulario y lógica para eliminar la cuenta permanentemente (actualmente el botón de perfil redirige aquí).
[ ] `app/empleos/page.tsx`: Mejorar el catálogo de búsqueda integrando filtros interactivos (por modalidad, jornada, categoría y localidad) en un componente `JobSearchCatalog.tsx`.
[ ] `app/recuperar-password/page.tsx`: Flujo de "Olvidé mi contraseña".

## 4. Rutas de Empresas / Reclutadores (En Progreso)
[x] `app/panel-empresa/page.tsx`: Dashboard de la empresa.
[x] `app/panel-empresa/ajustes/page.tsx`: Edición del perfil de la empresa (logo, portada, descripción).
[x] `app/panel-empresa/empleos/nuevo/page.tsx`: Formulario de publicación de nuevas ofertas laborales.
[ ] `app/panel-empresa/postulaciones/page.tsx`: Panel para que la empresa pueda ver a los candidatos que aplicaron a sus ofertas.
[ ] Añadir funcionalidad para que la empresa pueda cambiar el estado de la postulación (`pending` -> `reviewed` -> `accepted` / `rejected`).
[ ] `app/panel-empresa/empleos/page.tsx`: Listado de los empleos activos e inactivos de la empresa con opción a pausarlos, editarlos o eliminarlos.

## 5. Integración Lógica y Estado
[x] Auth System con Supabase y manejo de sesiones.
[x] Lógica de vistas y analíticas básicas (`jobs-stats.json`).
[ ] Integración del sistema de Storage para imágenes (logos y portadas) de empresas en el bucket `PROFILES`. (Los CVs ya están listos en `RESUMES`).

## 6. Detalles Finales, Seguridad y Mejoras Propuestas
[ ] Actualizar `README.md` (actualmente tiene el texto por defecto de create-next-app) con las instrucciones para levantar el proyecto Minú Empleos.
[ ] Enviar notificaciones (Emails o In-App) cuando el estado de una postulación cambia.
[ ] Implementar un sistema de verificación (`is_verified`) para destacar empresas confiables en la plataforma.
[ ] Mejorar SEO añadiendo meta tags dinámicas en las ofertas de empleo y perfiles de empresa.
[ ] Sanitización de inputs (Zod/DOMPurify) en las descripciones largas de los empleos para evitar inyecciones XSS y validación estricta de formularios.
[ ] Añadir y auditar políticas RLS (Row Level Security) robustas en las tablas de Supabase y en los buckets de Storage (como hicimos con el permiso DELETE de CVs) para máxima seguridad.
